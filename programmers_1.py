def solution(id_list, report, k):
    answer = []
    id_cp = id_list
    count = []
    for i in range(len(id_list)):
        for j in range(len(report)):
            at, dp = report[j].split()
            if id_list[i] == at and id:
                

    return answer